# DeepSeek key — lives only here, never reaches the browser.
DEEPSEEK_API_KEY=your_actual_key_here
DEEPSEEK_MODEL=deepseek-chat

# Comma-separated list. The portfolio and the Sổ Vé ticket book share this
# origin (they differ only by path), so one entry covers both.
# Add a localhost entry while developing against a local static server.
ALLOWED_ORIGIN=https://nguyenhoang88502.github.io,http://localhost:8777
